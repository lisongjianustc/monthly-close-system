<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mc.workflow.mapper.WfTaskMapper">

    <select id="selectPendingByAssigneeId" resultType="com.mc.workflow.entity.WfTask">
        SELECT * FROM wf_task
        WHERE assignee_id = #{assigneeId}
        AND status IN ('PENDING', 'IN_PROGRESS', 'PENDING_REVIEW', 'PENDING_APPROVE')
        ORDER BY sla_deadline ASC
    </select>

    <select id="selectByInstanceId" resultType="com.mc.workflow.entity.WfTask">
        SELECT * FROM wf_task
        WHERE instance_id = #{instanceId}
        ORDER BY sort ASC, id ASC
    </select>

</mapper>
